# Release Check — 2026-09-03 16:00 — 5-6-b (w56b-precise-hands-history)

Files Changed: 14 modified + 3 new (`src/core/asymmetry.ts`, `src/core/asymmetry.test.ts`,
`tests/e2e/precise-hands-history.spec.ts`). 864 +/134 −.

## Findings

- **Exposed secrets**: none. No keys/tokens/passwords. e2e seed uses only synthetic
  localStorage profiles.
- **Debug leftovers**: none (`console.log` / `TODO` / `debugger` grep clean on changed files).
- **Unintended files**: none. No `node_modules` / `.env` / build output. `dist/` untracked-only,
  gitignored.
- **Broken code**: none. `typecheck` + `build` + 208 unit tests + 10 Playwright tests all green.
- **Scope creep**: none. Changes are confined to the three brief items — `src/core` (task sort +
  new asymmetry), S1/S2/S3 screens, `WithinSubjectPanel`, session-store, i18n ko, css, docs.
  `src/adapt/*` untouched. Quick-measure path unchanged (flow.spec / adapt.spec green).
- **Missing tests**: no — `reports/eng/2026-09-03-5-6-b.md` has real result lines; new golden
  tests for both `src/core` changes; core coverage gate held (100/93.43/100/100).
- **i18n**: no user-facing literals outside `src/i18n` (Hangul grep on changed .ts = comments
  only). en key parity test green.
- **Schema**: stored Profile shape unchanged (`sessionId`/`asymmetry` already v2). No migration
  step needed. `docs/profile-format.md` + `CHANGELOG.md` updated.

## Verdict
PASS
