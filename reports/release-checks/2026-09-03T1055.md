# Release Check — 2026-09-03 10:55 (3B-b: 적응 화면 + S3 비교 패널)

Files Changed: 20 (신규 9, 수정 11) · +1684 / −20

## Findings
- No exposed secrets. 새 URL 은 공개 DOI / `github.com/wonkyard/ganeum` blob 링크뿐
  (`src/adapt/citations.ts`, CLAUDE.md 에 명시된 public 레포).
- No debug leftovers. diff 에 `console.*` / `TODO` / `debugger` 없음.
- No unintended files. `dist/` 는 gitignore, 스테이지 안 됨. 소스·테스트·문서만.
- No syntax errors. `typecheck` 통과, `build` 통과, 26 test files / 176 통과, e2e 4 통과.
- Scope: 브리프 4개 항목 + 테스트 + CHANGELOG/backlog 에 정확히 대응. 미사용 i18n 키
  1개(`result.comparisonReserved`) 제거 — 이번 변경으로 dead 가 된 것이라 범위 내.
- `src/adapt/{sizing,presets,inv-norm}.ts` API 불변 — `citations.ts`/`fitts-chart.ts` 는 add-only.
- 조건 기하 / `src/core/*` 측정 로직 미변경 (커버리지 게이트 100/92.93 유지).
- Medium: `tests/e2e/adapt.spec.ts` 가 착지 지터에 `Math.random()` 사용 →
  이론상 flaky 여지. 완화됨: 지터 ±6px 로 `weSource="measured"` 를 안정적으로 만들고
  퇴화 경로는 단위 테스트가 별도로 덮음. 로컬 3회 연속 초록.

## Verdict
PASS
