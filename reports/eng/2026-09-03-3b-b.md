# Eng — 2026-09-03 (3B-b)

Backlog item: 3B-b — 적응 화면(S4) + S3 비교 패널 + 배선 (`w3b-b-adapt-ui`)

Changed:
- `src/adapt/morph.ts` (신규) + `morph.test.ts` — We 축 프리셋 보간 + "나" 스냅, 순수 함수.
- `src/adapt/citations.ts` — `CITATION_URLS` + `ADAPT_MODEL_DOC_URL` (add-only).
- `src/adapt/index.ts` — morph 재노출.
- `src/render/fitts-chart.ts` — `overlays` 옵션 + `setOverlay(id, visible)` (add-only, 자가 드로잉).
- `src/ui/components/morph-slider.ts` · `sample-ui.ts` · `within-subject-panel.ts` (신규 + 각 테스트).
- `src/ui/screens/s4-adapt.ts` (신규) + `#/adapt/:id` 라우트(`src/app.ts`).
- `src/ui/screens/s3-results.ts` — 예약 슬롯 → `WithinSubjectPanel`, "화면 맞춰보기" 버튼 활성화.
- `src/ui/screens/s2-measure.ts` — `Profile.calibrated` / `viewport.pxPerMm` 를 `ganeum.calibration` 에서 채움.
- `src/i18n/ko.ts` — `adapt.*` / `result.compare*` 키, 미사용 `result.comparisonReserved` 제거.
- `src/styles/app.css` — S4 · 비교 패널 · 오버레이 스타일.
- `tests/e2e/adapt.spec.ts` (신규). `CHANGELOG.md` 갱신.

Tests:
- `npm run typecheck` — 통과 (에러 0).
- `npm test` — 26 files / 176 passed (신규: morph 19 + SampleUI 6 + MorphSlider 6 + WithinSubjectPanel 7).
- `npm run test:coverage` — core 게이트 유지: All files 100% stmts / 92.93% branch / 100% funcs / 100% lines (임계 90/85/90/90).
- `npm run build` — 통과 (`tsc --noEmit && vite build`, 54 modules, dist 정상).
- `npm run test:e2e` — 4 passed (기존 flow ×2, calibrate ×1, 신규 adapt ×1).

Done-when check: 충족.
- 측정 완주 → S3 "이 결과로 화면 맞춰보기" → `#/adapt/:id` → 슬라이더를 "손떨림"으로 밀면
  `--hit-size` 가 실제로 커짐(e2e `expect.poll`), "나" 스냅 마커 존재, "원래대로" 토글 → 44px.
- S3 비교 패널: 프리셋 회귀선 오버레이 토글(e2e에서 `.fitts-overlay[data-overlay-id=tremor]`
  `is-off` 해제 확인), 출처 링크 상시.
- `morph.ts` 골든 테스트 초록 (끝점=프리셋, 중간 보간, 스냅/클램프, 퇴화 경로).
- 사용자 대면 리터럴 0개, i18n `ko`+`en` 키 파리티 초록.
- `weSource="nominal-fallback"` → `buildMorphAxis` `meDisabled` → S4 "나" 비활성 + 안내(단위 테스트).

## Status
READY FOR RELEASE CHECK
