# Eng — 2026-09-03 (5-6-a)

Backlog item: 5-6-a — 교육 페이지(S6) + 적응 샘플 UI 2개 · 작업 브랜치 `w56a-education-samples`
회사 브리프: `reports/IDEA-20260901-1455/brief-5-6-a.md`

## Changed

신규:
- `src/ui/screens/s6-about.ts` — S6 "가늠이란?" 스크롤 문서 7섹션 + `#/about` 라우트.
  목차 `nav`, 섹션 랜드마크, 딥링크 스크롤 + 제목 포커스, 원형 8타깃 축소 삽화(정적 SVG).
- `src/ui/components/fitts-widget.ts` — 자체 SVG Fitts 위젯. 옵션객체 + `destroy()`.
  드래그(수평=A, 수직=W) + 키보드 대체 경로(슬라이더 2개 + 타깃 `role="slider"` 화살표키).
  `predictFitts()` 순수 함수(= `src/core/fitts.ts` 재사용). reduced-motion → 트랜지션만 off.
- `src/ui/components/fitts-widget.test.ts` — 예측 손검산 + 슬라이더/화살표키 갱신 + 클램프 (8 케이스).
- `tests/e2e/about.spec.ts` — 폰 `#/about` 7섹션 + 위젯 슬라이더 예측값 변화 + S0 링크 + 딥링크 포커스 (3).

수정:
- `src/ui/components/sample-ui.ts` — `kind: keypad|login|toolbar` 분기. 로그인 폼 / 미디어
  툴바 추가. 같은 `--hit-size`/`--gap`/`--pad` + 250ms. 기존 API 유지 + `getKind()`.
- `src/ui/screens/s4-adapt.ts` — `[키패드][로그인 폼][미디어 툴바]` 탭(`role="tab"`).
  전환 시 모드·sizing 유지. "왜?" 해설에 `#/about#adapt-model` 딥링크.
- `src/ui/screens/s3-results.ts` — "이게 무슨 뜻이죠?" 해설에 `#/about#fitts` 딥링크.
- `src/ui/screens/s0-home.ts` — "가늠이란?" 링크 활성화(스텁 제거) → `#/about`.
- `src/ui/router.ts` — 두 번째 `#`(화면 내 앵커) 분리 + `routeAnchor()`.
- `src/adapt/citations.ts` — MacKenzie 1992 / ISO 9241-411 / WCAG 2.5.5·2.5.8 추가 (**add-only**).
- `src/app.ts` — `#/about` 라우트 배선.
- `src/i18n/ko.ts` — `about.*` / `adapt.sampleTab.*` / `adapt.login*` / `adapt.toolbar*` /
  `result.explainMore` 키. `en` 은 파생(파리티 자동).
- `src/styles/app.css` — S6 + Fitts 위젯 + 로그인/툴바 목업 + 샘플 탭 스타일.
- `CHANGELOG.md`, `reports/backlog.md`.

미변경 확인: `src/core/*`, `src/adapt/{sizing,presets,morph,inv-norm}.ts` — 손대지 않음.
런타임 의존성 추가 0 (self-drawn SVG, 차트/애니 라이브러리 없음).

## Tests

- `npm run typecheck` → 통과 (에러 0).
- `npm test` → `Test Files 27 passed (27)` / `Tests 189 passed (189)` (기존 176 + 13).
  i18n 파리티 테스트 초록, core 커버리지 게이트(`src/core/**` only)는 core 미변경이라 유지.
- `npm run build` → `✓ built in ~0.5s`, 56 modules, dist 산출 정상 (sw.js precache 주입 포함).
- `npx playwright test` → `7 passed` (기존 4개: flow ×2, calibrate, adapt — 전부 초록;
  adapt 는 S4 샘플 탭 검증 추가. 신규 3개: about.spec.ts).

## Done-when check

- `#/about` 7섹션 스크롤, Fitts 위젯 드래그/슬라이더/화살표키로 예측 MT 갱신 — 충족
  (Playwright `about.spec.ts` + unit).
- S0 "가늠이란?" → `#/about` — 충족 (Playwright).
- S3/S4 딥링크가 해당 섹션으로 스크롤 + 제목 포커스 — 충족 (Playwright 딥링크 케이스).
- S4 샘플 탭 3개, 전환 시 목업 렌더 + 슬라이더 CSS 변수 재배치 + 토글 — 충족
  (Playwright `adapt.spec.ts`, unit `sample-ui.test.ts`).
- typecheck / test / build 초록, core 커버리지 게이트 유지 — 충족.
- 신규 단위테스트(`fitts-widget` 계산, `sample-ui` 3종) — 충족.
- Playwright 폰 뷰포트 신규 + 기존 전부 초록 — 충족.
- 사용자 대면 리터럴 0 (SVG 아이콘 글리프·수식 `MT = a + b·log2(...)` 리터럴은
  장식/수식 표기라 i18n 대상 아님) — 충족.
- `src/core/*` · `src/adapt/{sizing,presets,morph,inv-norm}.ts` 미변경, citations add-only — 충족.

메모(범위 밖 — `project-lead` 앞): 위젯 드래그의 2축 매핑(수평 A · 수직 W)은 데스크톱에서
직관적이나 터치에서 스크롤과 겹칠 수 있다. `touch-action: none` 을 SVG 에 걸어 완화했지만
5-6-b 접근성 감사에서 실기 확인 권장.

## Status

READY FOR RELEASE CHECK
