# Eng — 2026-09-02

Backlog item: 주 1–2 — 뼈대 + 측정 엔진 + 테스트/CI

## Changed

Repo setup
- `package.json` (dev deps only: vite, typescript, vitest, @vitest/coverage-v8,
  jsdom, @playwright/test, @types/node), `tsconfig.json` + `tsconfig.node.json`,
  `vite.config.ts` (`base: "./"` — portable Pages/도메인/`file://`),
  `vitest.config.ts` (node 기본 환경, core 커버리지 게이트 90/85), `playwright.config.ts`.
- `.gitignore` (node_modules, dist, coverage, .env*, playwright 산출물).
- `index.html` (skip-link, color-scheme meta, manifest link).
- `public/manifest.webmanifest`, `public/icon.svg`, `public/sw.js`
  (의존성 0 수동 service worker — navigate=network-first, asset=cache-first, 버전 회전).
- `.github/workflows/ci.yml` (verify: typecheck / test:coverage / build + e2e job),
  `.github/workflows/deploy.yml` (main → Pages).

`src/core/` (프레임워크 무관 순수 함수)
- `stats.ts` mean/median/표본SD(n-1)/MAD, `fitts.ts` `ID=log2(A/W+1)` + 예측 MT,
  `regression.ts` 최소제곱 + r², `outliers.ts` 중앙값 ±3·MAD, `throughput.ts`
  We=`4.133·SDx`/IDe/TP (+ 산포 0 시 명목 W 폴백), `task.ts` ISO 원형 배열 +
  criss-cross 순서 + 조건 설계(quick 3 / precise 9), `ids.ts` 시간정렬 ID +
  조건 ID, `migrate.ts` 스키마 버전 + 단계별 마이그레이션 + FutureSchemaError,
  `analyze.ts` 워밍업 제거 → 이상치 제거 → 회귀 → 처리율 파이프라인, `types.ts`, `index.ts`.

`src/ui/` — `store.ts` (46줄 반응형 스토어), `router.ts` 해시 라우터(파라미터/폴백/중복 억제).
`src/render/` — `target-field.ts` S2 Canvas: 원형 배열, 다음 타깃 맥동, 히트/미스,
  Pointer Events + `getCoalescedEvents` 궤적, DPR 리사이즈, `destroy()`.
`src/a11y/` — `reduced-motion.ts` (matchMedia 없으면 폴백), `theme.ts` (system/light/dark,
  localStorage try/catch), `styles/tokens.css` (다크/라이트 토큰, `:focus-visible` 링,
  reduced-motion 시 모션 0).
`src/i18n/` — `ko.ts`(채움) / `en.ts`(키만, 빈 값은 ko 폴백) / `index.ts` `t()` 파라미터 치환.
`src/app.ts` + `src/main.ts` — 홈 → 측정 → 결과(수치) 배선. 개발 빌드에만 `window.__ganeum`
  자동화 훅(프로덕션 번들에서 tree-shake 확인).
`docs/` — `profile-format.md` (안정 계약 v1), `adr/0001-no-runtime-dependencies.md`.

## Tests

- `npm run typecheck` (tsc -p tsconfig.json && -p tsconfig.node.json) → 통과, 0 에러.
- `npm test` (`vitest run`) → **14 파일 / 69 테스트 전부 통과** (~2.7s).
  골든 케이스: 회귀 a/b/r²(손계산 1.3/0.9/0.81), ID Shannon(MacKenzie A=192,W=24→log2 9),
  We/IDe/TP(손계산), MAD 이상치, criss-cross 순서, 마이그레이션, analyzeSession 완주 시 (a,b,r²,TP).
- `npm run test:coverage` → core 커버리지 통과 (Stmts 100 / Funcs 100 / Lines 100 /
  Branch 93.28 — 게이트 90/85). barrel `index.ts` 는 제외.
- `npm run build` (`tsc --noEmit && vite build`) → 통과. dist 13.8 kB JS(gzip 6.2) +
  2.4 kB CSS. sw.js / manifest / icon 이 dist 에 포함.
- `npx playwright test` → **1 통과** (3.5s). 홈 → 측정 → `#/results` 에서
  `MT = a + b·ID`, `r²`, `bits/초` 표시 확인.

## Done-when check

- 마우스로 한 세션 완주 → (a, b, r², TP) 표시: **met**. Playwright 스모크가
  측정 세션(조건 3 × 11탭)을 완주시키고 결과 화면의 회귀식·r²·처리율을 검증.
  훅은 실제 `pointerdown` 과 동일한 `registerTap` 경로를 탄다.
- typecheck + test + build 통과: **met** (위 결과).
- core 골든 케이스가 ISO/MacKenzie 수치에 통과: **met**.
- CI 워크플로가 세 단계를 실행: **met** (`.github/workflows/ci.yml`).

## Status

READY FOR RELEASE CHECK
