# Eng — 2026-09-02 (3A 라운드 1)

Backlog item: 3A — 측정 → 결과 → 카드 (+ 엔진/저장 수정) · 브랜치 `w3a-measure-results`
Brief: 회사 레포 `reports/IDEA-20260901-1455/brief-3A.md`

## Changed

Phase 0 (엔진/데이터 정확성 — 순수 core/render + 골든 테스트):
- `src/core/types.ts` — 스키마 v2: `Tap.devAxis/devOrtho`, `Condition.displayedA/Ae`,
  `Profile.we/weSource/sessionId/appVersion`, `WeSource` 타입.
- `src/core/throughput.ts` — `pooledEffectiveWidth()` (전 조건 축투영 오차 pooled SD × 4.133).
- `src/core/analyze.ts` — `devAxis` 소비, pooled We, `Ae` 소비, errorRate = 놓친 타깃/전체
  타깃, 축투영 이상치 제거, 신뢰도 게이트, `{ status: "ok" | "insufficient" }` (throw 제거).
- `src/core/task.ts` — `designConditions(mode, ref, pointerType)`; touch W 바닥값 24px.
- `src/core/migrate.ts` — v1 → v2 스텝 (필드 채움 + taps 로 We 재계산).
- `src/render/target-field.ts` — P0-1 접근 축 투영(직전 타깃 추적), P0-4 타깃당 첫 press
  만 엔드포인트/press→press, P0-9 조건 진행 중 정사각 고정 + `visualViewport`.

Phase 1 (저장):
- `src/storage/profiles.ts` (신규) — Profile 배열/prefs/lastId, try·catch, 쿼터 처리,
  `deleteAllData`, `exportProfileJSON`, `migrateProfile` 통과.

Phase 2 (화면 + 컴포넌트):
- `src/app.ts` — 라우터 배선 + 에러 경계만.
- `src/ui/screens/{s0-home,s1-setup,s2-measure,s3-results,s5-card}.ts` (신규).
- `src/ui/components/{app-modal,countdown,count-up-number,stat-tile,disclosure,
  theme-toggle,lang-toggle,top-bar}.ts` (신규), `src/ui/{dom,screen,session-store}.ts`.
- `src/render/{fitts-chart,result-card}.ts` (신규).
- `src/ai/rules.ts` (신규) — 규칙 기반 해설.

크로스커팅:
- `src/i18n/{ko,en,index}.ts` — 전 리터럴 이관, `en` 키 파생, `Intl` 포맷터.
- `src/styles/{tokens,app}.css` — @font-face(자가 호스팅 WOFF2), overscroll,
  user-select, 전 화면/컴포넌트 스타일.
- `src/assets/fonts/` — JetBrains Mono(OFL) 서브셋 400/700 + OFL.txt.
- `build/sw.template.js` + `vite.config.ts` 플러그인 — 빌드 산출 에셋 precache, 캐시명 해시.
- `docs/profile-format.md` v2, `CHANGELOG.md` (신규).
- `tests/e2e/flow.spec.ts` (신규, 폰+데스크톱), 옛 스모크 2개 삭제.

## Tests

- `npm run typecheck` — 통과 (`tsc -p tsconfig.json` + `tsconfig.node.json`).
- `npm test` — `Test Files 18 passed (18) / Tests 108 passed (108)`.
- `npm run test:coverage` — core 게이트 통과: `All files 100% stmts / 92.93% branch /
  100% funcs / 100% lines` (게이트 90/85/90/90).
- `npm run build` — 통과. `dist/index-*.js 44.20 kB (gzip 16.79)`, `sw.js` 프리캐시
  8개 항목 주입 확인, `node -c dist/sw.js` OK.
- `npx playwright test` — `2 passed` (phone 390×844 + desktop 1280×800, 홈→측정→결과→카드
  실클릭 완주, 회귀선 path 1개, "지난 결과 (1)").

골든 테스트: P0-1 `target-field.test.ts` (축투영 = 축 성분, 대각 이동에서 dx≠devAxis),
P0-2 `analyze.test.ts`/`throughput.test.ts` (pooled SD × 4.133), P0-3 `analyze.test.ts`
(Ae 다르면 TP 다름), P0-4 `target-field.test.ts` (미스+재시도 → 엔드포인트 1개, press→press),
P0-8 `migrate.test.ts` (v1 → v2).

## Done-when check

- 노트북(마우스) + 폰 뷰포트에서 홈 → 빠른 측정 완주 → 결과(회귀선 자가 드로잉·처리율
  카운트업) → 카드 PNG 버튼·JSON export → 홈 "지난 결과 (1)": **met** (Playwright 두 뷰포트).
- typecheck / test / build 초록: **met**.
- 골든 테스트 P0-1/2/3/4/8: **met**.
- 사용자 대면 리터럴 i18n 0개, `en` 키 파리티: **met** (i18n.test.ts + 소스 스캔).
- 비행기 모드 새로고침: **구조상 met, 실기기 수동 확인 대기** — `sw.js` 가 빌드 산출
  전 에셋을 프리캐시하고 fetch 는 캐시 우선. 실제 노트북/안드로이드 airplane-mode
  reload 는 배포 후 수동 확인 항목.

범위 밖(3B): SC 보정, `src/adapt/`, S4, S3 `WithinSubjectPanel` 내용, S6, 정밀 측정,
양손, `ABMiniTest`, AI LLM 백엔드 — 손대지 않음.

## Status

READY FOR RELEASE CHECK
