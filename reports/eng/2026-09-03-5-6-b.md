# project-eng — 2026-09-03 — 5-6-b: 정밀 측정 + 좌우손 비교 + 히스토리 비교

작업 브랜치 `w56b-precise-hands-history` (base `main` @ 2c75d94). 회사 브리프
`IDEA-20260901-1455/brief-5-6-b.md`. backlog 항목 `2026-09-03 — 5-6-b`.

## 한 것

### 1. 정밀 측정 (조건 9개)
- `src/core/task.ts` — `designConditions("precise")` 반환을 **ID 오름차순**으로 고정
  (쉬움 → 어려움 램프). 9조건 격자·포인터타입 바닥값(touch 24 / mouse 12)은 기존
  구현 유지. quick 은 그대로 어려운 조건 먼저.
- `src/ui/screens/s1-setup.ts` — 정밀 카드 활성화, 카드 택1 실제 토글, 정밀 선택 시
  "양손 비교" 체크박스. 카드 포커스 중 스페이스는 선택용(시작 아님).
- `src/ui/screens/s2-measure.ts` — 조건 수 모드별(3/9), 진행 도트 9 반영, 힌트 quick 전용.
- `src/ui/session-store.ts` — `bothHands` 필드.

### 2. 좌우손 비교 (정밀 전용)
- `src/core/asymmetry.ts` (신규) — `computeAsymmetry(right, left)` = (R−L)/mean,
  한 손이라도 없으면 `null`.
- `s2-measure.ts` — 양손 세션은 `runHand()` 반복. 첫 손 완주 → 인앱
  `.screen-interstitial` "손을 바꾸세요" (브라우저 dialog 없음, aria-live + 제목 포커스)
  → "준비됐어요" → 둘째 손 → 결과. 두 Profile 공유 `sessionId`, 완주 후 `asymmetry`
  채워 재저장. 배열 상한 20 유지.
- `s3-results.ts` `deriveHandComparison()` + `WithinSubjectPanel.handComparison` →
  "왼손 X / 오른손 Y · 비대칭 N%" 라인.

### 3. S3 "지난 측정 대비"
- `s3-results.ts` `deriveHistory()` — 같은 hand + 같은 mode, 먼저인 다른 세션. 직전 =
  가장 최근.
- `WithinSubjectPanel.history` — 직전 회귀선 오버레이(`prev`) + 토글 칩(날짜),
  "지난 측정 대비 ±X bits/초 (↑/↓/거의 같음)", 3회+면 자체 SVG 스파크라인
  (라이브러리 0), 저신뢰 점 회색 + 문구.

### 문서 / i18n
- `docs/profile-format.md` — 양손 세션 = 두 Profile 공유 `sessionId` 규칙 명시.
  스키마 형태 불변.
- `CHANGELOG.md` — 5-6-b 섹션.
- 새 문자열 전부 `src/i18n/ko.ts`. en 파리티 유지(값 ko 폴백).

## 테스트 결과 (실제 출력)

- `npm run typecheck` — 통과 (tsc --noEmit, app + node 프로젝트 둘 다).
- `npm test` — **28 files / 208 tests passed** (기존 200 + asymmetry 6 + task 정밀 5 +
  within-subject-panel 좌우손/시점 9, 일부 파일 중복 카운트 제외).
- `npx vitest run --coverage` — core 게이트 유지: All files **100 / 93.43 / 100 / 100**
  (stmt/branch/func/line) vs 임계 90/85/90/90. `asymmetry.ts` 100/90/100/100,
  `task.ts` 100/96/100/100.
- `npm run build` — 통과 (tsc --noEmit && vite build, 57 modules, dist 생성).
- `npx playwright test` — **10 passed** (기존 7 + 신규 3):
  - `precise-hands-history.spec.ts:54` 정밀 9조건 완주 → 결과, 진행 도트 9개
  - `precise-hands-history.spec.ts:72` 정밀 + 양손 인터스티셜 → 둘째 손 → 비대칭 타일 +
    두 Profile sessionId 연결 (localStorage 확인)
  - `precise-hands-history.spec.ts:108` 과거 세션 2개 localStorage 시드 → 델타 라인 +
    직전 오버레이 칩(5번째) + 스파크라인(circle 3)

## done-when 대조

- [x] S1 정밀 활성 → 9조건 완주 → S3. 진행 표시 9개. (e2e)
- [x] 정밀 + 양손: 첫 손 → "손 바꾸세요" → 둘째 손 → S3 비대칭 타일. sessionId 연결. (e2e)
- [x] 같은 손·모드 과거 세션 → 직전 회귀선 토글 + "지난 측정 대비 ±X" + (3회+) 스파크라인. (e2e + unit)
- [x] `designConditions("precise")` + `computeAsymmetry()` 골든 테스트 초록. core 게이트 유지.
- [x] typecheck / test / build 초록.
- [x] Playwright 기존 7 + 신규 3 초록. 폰 뷰포트.
- [x] 사용자 대면 리터럴 0 (Hangul 리터럴 grep = 주석만). i18n 파리티 초록.
- [x] `src/adapt/*` 미변경. 빠른 측정 경로 회귀 없음 (flow.spec / adapt.spec 초록).

READY FOR RELEASE CHECK.
